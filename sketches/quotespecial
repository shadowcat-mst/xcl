let x = `q`!foo 'bar' "baz" quux!;

let x = .unindent `q`{{{
  stuff
  goes
  here
}}}:

# q{{ } } }};
# `q`{{ } } }}; <- ' } } '

#{{{

  let str = .unindent `q`'''
    triple
    quoted
    string
  ''';

}}}#
