let x = `q`!foo 'bar' "baz" quux!;

let x = .unindent `{{{`
  stuff
  goes
  here
}}}:

# q{{ } } }};
# `q`{{ } } }}; <- ' } } '

#{{{

  let str = .unindent q`'''`
    triple
    quoted
    string
  ''';

  let data = xml`<<<`
    <foo />
  >>>;

}}}#
